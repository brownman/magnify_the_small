--ex1
CREATE TABLE notebook (
    id INTEGER PRIMARY KEY,
    doing TEXT,
    should TEXT,
    sport INTEGER
);

CREATE TABLE memos(
    id INTEGER PRIMARY KEY,
    priority INTEGER,
    task TEXT
);
